import os, sys, math

x = 10

def Bad_Function_Name( A, B, C, D, E, F ):
    global x
    l = 1; O = 0
    if A == True:
        if B == False:
            if C == None:
                try:
                    print(eval("A + B"))
                    res = E[0] + F + l + O
                except:
                    pass
    else:
        return None

Bad_Function_Name(True, False, None, 1, [2], 3)